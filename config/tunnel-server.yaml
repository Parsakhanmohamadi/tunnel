listen_addr: ":8443"

# TLS certificate and key paths on the abroad server
tls_cert_file: "/etc/customtunnel/server.crt"
tls_key_file: "/etc/customtunnel/server.key"

# UDP address of the WireGuard interface on the abroad server
wireguard_remote: "127.0.0.1:51820"

